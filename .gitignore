# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
*.so

# Environments
.venv/
venv/
env/
.env
.env.*
!.env.example

# Editors / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/

# Node / frontend (in case the chat UI is JS/TS)
node_modules/
.next/
.turbo/
.vite/
*.log

# Models & artifacts (never commit weights)
models/
*.safetensors
*.gguf
*.bin
.hf_cache/
huggingface/

# Local data (SQLite history db, etc.)
data/

# Benchmark run output (per-run, disposable). Tracked baselines live in
# bench/baselines/ and are NOT ignored — see Phase C of the v1 ship plan.
bench/results/
!bench/baselines/

# Claude Code local settings
.claude/settings.local.json

# Transient session-handoff doc (local only — read on session start, delete after acting)
RESUME.md

# Git worktrees (configured in CLAUDE.md "Quick reference")
.worktrees/
